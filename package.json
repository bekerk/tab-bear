{
  "name": "tab-bear",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/background.ts src/content.ts --outdir dist --target=browser",
    "dev": "bun build src/background.ts src/content.ts --outdir dist --watch --target=browser",
    "bundle": "./bundle_dist.sh"
  },
  "dependencies": {
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.32",
    "@types/turndown": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}